version: '3.8'

services:
  cfa-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cfa-assistant-container
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped
